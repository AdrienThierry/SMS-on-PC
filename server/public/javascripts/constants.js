// --------------------------------------------------
// Expose constants
// --------------------------------------------------
angular.module('SMS_on_PC').factory('constants', function() {
	var constants = {
		shared_conf_name: "conf",
		device_id_var_name: "device_id"
	}

    return constants;
});
