/**
 * Constants specific to the server
 */

var constants = {
	config_file_name: "conf.json",
	config_route: "conf",
	last_id_file_name: "last_id",
	mdns_query_period: 1000
};

module.exports = constants;
